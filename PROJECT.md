# 项目介绍

林海漫游，一个专精软件工程和渗透攻击的AI Agent

# 项目ROADMAP

- [x] 完成工具类（基础工具已实现）
- [x] 完成LLM对接部分
- [x] 完成Agent逻辑
- [x] 使用浏览器拉取微信公众号/知乎回答/...等文章并自动转为markdown
- [x] 改进TUI
- [ ] 编写webui

# Agent设计

## 架构设计

Agent具有以下功能

- 工具调用：调用各类的工具
- 插件修正：针对不同的LLM(deepseek/kimi/minimax)插入对应prompt，修正agent行为
- 历史压缩：在上下文过长时删除部分消息
- 用户响应：用户可以随时打断agent输出，调整agent行为
- 全局指导：在单独的文件中保存用户的偏好等，Agent在运行时会动态修改这个全局指导
    - 在开始时全局指导只保存用户使用的语言（中文/英文/...）

## 工具调用

### 设计

Agent使用Markdown JSON代码块调用工具，例如：

```json toolcall
{"name": "tool_name", "arguments": {...}}
```

和标准的OpenAI函数调用的区别在于：
- agent可以一次性调用多个工具
- 工具的输出可以流式展示在用户的界面
    - 用户可以实时看到agent填写函数调用的过程
- 未来可以实现在agent输出的同时检查/调用工具


# 消息设计

我们需要将文件内容/动态prompt/命令执行结果/全局指导等内容传给agent， 但是这些内容只能使用role=user的消息表示

为了突破这个限制，我们在代码设计中使用多种Message类表示各种数据，并在传递给LLM时使用对应的tag包裹
